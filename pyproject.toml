[project]
name = "c964-movie-recommender"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "numpy",
    "scikit-learn",
    "python-dotenv",
    "pandas>=3.0.3",
]
